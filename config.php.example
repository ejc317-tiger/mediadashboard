<?php
/** Optional host override; database credentials are fixed in db.php. */
return [
    'host' => getenv('DB_HOST') ?: 'localhost',
    'port' => getenv('DB_PORT') ?: '3306',
    // Keep the real key in the gitignored config.php or server environment.
    'openai_api_key' => getenv('OPENAI_API_KEY') ?: '',
];
